const prompt = require ('prompt-sync')();

var numero1, numero2, versuma;
    numero1 = parseInt(prompt('Primer numero: '));
    numero2 = parseInt(prompt('Segundo numero: '));

    versuma = numero1 + numero2;
    console.log('Suma: '+ versuma);
